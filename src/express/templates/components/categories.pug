ul.themes(class=additionalClass)
  each category in categories
    if (currentCategory)
      - var additionalClass = category.title === currentCategory.title ? 'themes__item-link--active' : '';
    li.themes__item
      a.themes__item-link(href='/articles/category/' + category.id class=additionalClass)
        | #{category.title}
        sup #{category.count}
      button.themes__remove-btn(type='button') Удалить категорию
